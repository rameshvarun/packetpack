{
  "name": "packetpack",
  "version": "0.0.1",
  "description": "A library for compressing WebSocket and WebRTC DataChannel messages.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "prettier": "^2.8.7",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "format": "prettier --write src/*.ts test/*.ts",
    "build": "rm -rf lib/ && ./scripts/build-lz4.sh && tsc && cp src/lz4.js lib/lz4.js"
  },
  "dependencies": {
    "ffi-napi": "^4.0.3",
    "type-fest": "^3.7.2"
  }
}
